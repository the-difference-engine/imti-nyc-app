<div class="application">
  <h1>SECTION F: DOCUMENTS</h1>
  <!-- <p>Please see your attached transcripts, recommendations and/or references below.</p> -->
  <p><a href="/applications/<%= @application.id %>/documents/new">Add new document</a></p>
  <hr>

  <h3>You'll need the following:</h3>
  <% document_lists.each do |document| %>
  <p><%= document[0] %></p>
  <% end %>

  <table>
    <thead>
      <tr>
        <th>Document Name</th>
        <th>Attachment</th>
        <th class="upload">Upload</th>
      </tr>
    </thead>
    <tbody>
      <% document_lists.each do |document| %>
        <tr>
          <%= form_for [@application, @document] do |f| %>
          <td><%= document[0] %></td>
          <% if @documents.find_by(category: document[1]) %>
            <td><%= file_name(@documents, document[1]) %></td>
          <% else %>
            <td> </td>
          <% end %>
          <td>
              <%= f.file_field :attachment %>
              <%= f.hidden_field :category, value: document[1] %>
              <%= f.submit :save %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <hr>

  <h3>Other Application Sections</h3>
  <p>Please double check all sections below to ensure that your application is accurate.</p>
  <h4><a href="/applications/<%= @application.id %>">A. My Personal Data, Pay Application Fee & Submit</a></h4>
  <h4><a href="/applications/<%= @application.id %>/educations">B. Education</a></h4>
  <h4><a href="/applications/<%= @application.id %>/montessori_trainings">C. Montessori Trainings</a></h4>
  <h4><a href="/applications/<%= @application.id %>/work_experiences">D. Work Experience</a></h4>
  <h4><a href="/applications/<%= @application.id %>/references">E. References</a></h4>
  <h4><a href="/applications/<%= @application.id %>/documents">F. Documents</a></h4>
</div>
